# code-not-code README

The least annoying vscode theme

built with:

```console
npm install -g yo generator-code
```

```console
yo code
```


packaged with:

```console
npm install --global @vscode/vsce
```

```console
vsce package
```

installed with:

vscode command: "Install from VSIX"